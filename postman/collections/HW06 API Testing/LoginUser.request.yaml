$kind: http-request
name: LoginUser
description: ""
url: "{{baseUrl}}/api/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: "{\r

    \  \"email\": \"test@eshop.com\",\r

    \  \"password\": \"Test1234!\"\r

    }"
scripts:
  - type: afterResponse
    code: "pm.test(\"Login User thành công\", () => {\r

      \  pm.response.to.have.status(200);\r

      });\r

      \r

      const json = pm.response.json();\r

      \r

      pm.expect(json.token).to.be.a(\"string\").and.not.empty;\r

      pm.expect(json.user.role).to.eql(\"user\");\r

      \r

      pm.environment.set(\"userToken\", json.token);"
    language: text/javascript
order: 1787068018239
