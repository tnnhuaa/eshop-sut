# HW05 Performance Testing Workspace

Current state: calibration accepted at `L = 10`; Load `_002`, Stress, corrected Spike, and Soak `_002` are `HUMAN_VERIFIED`. Load `_002` replaces the contaminated Load `_001` evidence.

## Scenario B

Login → Search → Product Detail → Add/Get Cart → Apply Fixed Coupon → Checkout → Record Coupon Usage → My Orders.

## Safe start

1. Add the four performance-video URLs and the Agent Skill demonstration link.
2. Complete the AI analysis, human-review form, and 200–300-word AI Critique.
3. Run the final submission audit before packaging.

Never overwrite raw evidence or mark AI-generated material as human-verified.
