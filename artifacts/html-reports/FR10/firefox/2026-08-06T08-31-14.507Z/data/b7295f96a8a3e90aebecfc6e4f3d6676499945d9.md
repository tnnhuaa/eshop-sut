# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fr10\fr10-state-machine.spec.ts >> FR10-DT-012 — canceled order has no outgoing admin action
- Location: tests\fr10\fr10-state-machine.spec.ts:106:3

# Error details

```
Error: apiRequestContext.post: read ECONNRESET
Call log:
  - → POST http://127.0.0.1:3000/api/login
    - user-agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:153.0) Gecko/20100101 Firefox/153.0
    - accept: */*
    - accept-encoding: gzip,deflate,br
    - content-type: application/json
    - content-length: 50

```

# Test source

```ts
  1  | import type { APIRequestContext, Page } from "@playwright/test";
  2  | import { expect } from "@playwright/test";
  3  | import { environment } from "./environment.js";
  4  | 
  5  | type Role = "user" | "admin";
  6  | 
  7  | export async function authenticateViaApi(
  8  |   page: Page,
  9  |   request: APIRequestContext,
  10 |   role: Role,
  11 | ): Promise<string> {
  12 |   const credentials = role === "admin" ? environment.admin : environment.user;
> 13 |   const response = await request.post(`${environment.apiBaseUrl}/login`, {
     |                                  ^ Error: apiRequestContext.post: read ECONNRESET
  14 |     data: credentials,
  15 |   });
  16 |   expect(response.ok(), `API login failed for ${role}`).toBeTruthy();
  17 |   const body = (await response.json()) as { token: string };
  18 |   const storageKey = role === "admin" ? "adminToken" : "token";
  19 |   await page.addInitScript(
  20 |     ({ key, token }) => localStorage.setItem(key, token),
  21 |     { key: storageKey, token: body.token },
  22 |   );
  23 |   return body.token;
  24 | }
  25 | 
```